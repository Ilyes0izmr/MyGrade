<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- RecyclerView to display multiple modules -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewModules"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="16dp"
        android:scrollbars="vertical" />

    <!-- Button to calculate GPA for all modules -->
    <Button
        android:id="@+id/calculateGPAButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Calculate GPA" />


    <!-- TextView to display the final GPA result -->
    <TextView
        android:id="@+id/textViewFinalGPA"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Final GPA: --"
        android:textSize="18sp"
        android:gravity="center"
        android:paddingVertical="12dp"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/gpaResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GPA: N/A"
        android:textSize="18sp"
        android:layout_marginTop="10dp"
        android:textStyle="bold"/>


</LinearLayout>
